=>Javascript execution context
    - {} => Global execution context(GEC)
        and GEC is refered as this(stores GEC) keyword
        In browser this is window object

    - Function Execution Context
    - Eval Execution Context


    - {} => Global execution context(GEC)
        runs in two phases[Memory creation phase/mermory phase/creation phase[allocate space for variables] , Execution phase]

js is singlethreaded


=>queryselector all return nodelist
=>nodelist is different from list we cannot directyly apply list properties on nodelist

Promise :-
- pending : initial state, neither fullfilled not rejected
- fulfilled : meaning that the operation was compleated successfully
- rejected : meaning that the operation failed
